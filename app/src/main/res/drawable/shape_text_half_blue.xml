<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- 圆角半径 -->
    <corners
        android:topRightRadius="@dimen/btn_shape"
        android:bottomRightRadius="@dimen/btn_shape" />
    <!-- 填充颜色 -->
    <solid android:color="@color/btn_clickable"/>

</shape>